# GitHub repository metadata

Use this metadata when publishing `qualixar/jev-decision-layer`. It is a discoverability checklist, not a promise of stars, search ranking, or token savings.

- **Repository description:** `Typed, calibrated decision layer for coding agents: one runtime, host adapters for Codex, Claude Code, Antigravity, Hermes and VS Code, with an optional local Laya route.`
- **Topics:** `jev`, `typesafe-ai`, `mcp`, `ai-agents`, `agent-routing`, `coding-agents`, `codex-plugin`, `claude-code-plugin`, `vscode`, `laya-mlx`, `qualixar`
- **Social preview upload:** [`assets/social-preview.png`](assets/social-preview.png) (1280 × 640 PNG, solid background).
- **Primary language and license:** Python and MIT; upstream notices remain separate.
- **README lead:** name the actual decision the layer makes, show one runtime behind per-host adapters, give an executable first-use path that costs nothing, and link the limitations beside the claims.

Lead with the adapter story rather than a single host. The repository's claim is that the decision logic does not fork per harness, so the description, topics and README opening should all be readable by someone arriving from any of the five — a visitor who only uses VS Code should not have to work out whether this is a Codex product.

GitHub [topics](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/classifying-your-repository-with-topics) help classify repositories for discovery. GitHub recommends a small PNG/JPG/GIF [social preview](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/customizing-your-repositorys-social-media-preview) for shared links. The `llms.txt` and `capabilities.json` files help an agent read this repository; they are not guaranteed GitHub SEO mechanisms. Do not paste Schema.org JSON-LD into the README and claim GitHub executes it.
