"""Qualixar Jev Decision Layer: workspace-scoped 1.0.7 runtime.

The single place this package states its release. Everything that reports a
version to a host imports it from here, because six releases shipped while
every server answered 1.0.0 -- the docstring was bumped by hand and the value
beside it was not.
"""
__version__ = '1.0.7'
